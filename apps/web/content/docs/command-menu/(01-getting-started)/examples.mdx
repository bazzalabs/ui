---
title: Examples
section: Examples
summary: "Interactive examples and use cases for the command menu."
---

## Basic

A simple command menu with a few items. Press the button or use the keyboard shortcut to open.

<ComponentFrame
  containerClassName='sm:h-64'
  previewClassName='w-full flex justify-center'
  src="components/examples/command-menu/basic.tsx"
  lazy
>
  <Examples.CommandMenu.CommandMenu_Basic />
</ComponentFrame>

## Groups

Organize commands into logical groups with headings.

<ComponentFrame
  containerClassName='sm:h-64'
  previewClassName='w-full flex justify-center'
  src="components/examples/command-menu/groups.tsx"
  lazy
>
  <Examples.CommandMenu.CommandMenu_Groups />
</ComponentFrame>

## Submenus (Paged Navigation)

Command menus use paged navigation with breadcrumbs. Select a submenu to navigate into it, use the back button or Escape to return.

<ComponentFrame
  containerClassName='sm:h-84'
  previewClassName='w-full flex justify-center'
  src="components/examples/command-menu/submenus.tsx"
  lazy
>
  <Examples.CommandMenu.CommandMenu_Submenus />
</ComponentFrame>

## Nested Submenus

Multiple levels of paged navigation with breadcrumbs showing the full path.

<ComponentFrame
  containerClassName='sm:h-96'
  previewClassName='w-full flex justify-center'
  src="components/examples/command-menu/nested-submenus.tsx"
  lazy
>
  <Examples.CommandMenu.CommandMenu_NestedSubmenus />
</ComponentFrame>

## Keyboard Shortcuts

Multiple keyboard shortcuts can trigger the same command menu. This example supports `Cmd+K`, `Ctrl+K`, and `/`.

<ComponentFrame
  containerClassName='sm:h-84'
  previewClassName='w-full flex justify-center'
  src="components/examples/command-menu/keyboard-shortcuts.tsx"
  lazy
>
  <Examples.CommandMenu.CommandMenu_KeyboardShortcuts />
</ComponentFrame>

## Vim Bindings

Enable vim-style navigation with `Ctrl+j/k/n/p` in addition to arrow keys.

<ComponentFrame
  containerClassName='sm:h-96'
  previewClassName='w-full flex justify-center'
  src="components/examples/command-menu/vim-bindings.tsx"
  lazy
>
  <Examples.CommandMenu.CommandMenu_VimBindings />
</ComponentFrame>

## Icons & Descriptions

Add visual context with icons from Lucide React and helpful descriptions for each command.

<ComponentFrame
  containerClassName='sm:h-96'
  previewClassName='w-full flex justify-center'
  src="components/examples/command-menu/with-descriptions.tsx"
  lazy
>
  <Examples.CommandMenu.CommandMenu_WithDescriptions />
</ComponentFrame>

## Async Loading (React Query)

Integrate with TanStack Query for async data loading. This example demonstrates:
- Async submenu loading with caching
- Server-side search with query parameters
- Loading states handled by React Query

<ComponentFrame
  containerClassName='sm:h-96'
  previewClassName='w-full flex justify-center'
  src="components/examples/command-menu/linear-async.tsx"
  lazy
>
  <Examples.CommandMenu.CommandMenu_LinearAsync />
</ComponentFrame>

<ComponentFrame
  containerClassName='sm:h-96'
  previewClassName='w-full flex justify-center'
  src="components/examples/command-menu/linear-async-swr.tsx"
  lazy
>
    <Examples.CommandMenu.CommandMenu_LinearAsyncSWR />
</ComponentFrame>


## Streaming Deep Search

Stream results as they load during deep search across multiple submenus. Features:
- Multiple async loaders executing in parallel
- Progressive result display (streaming mode)
- Results appear as each submenu completes loading
- Try searching for items across all categories (e.g., "apple", "egg", "fish")

<ComponentFrame
  containerClassName='sm:h-96'
  previewClassName='w-full flex justify-center'
  src="components/examples/command-menu/async-submenus-streaming.tsx"
  lazy
>
  <Examples.CommandMenu.CommandMenu_AsyncSubmenusStreaming />
</ComponentFrame>
