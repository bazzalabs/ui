---
'@bazza-ui/react': minor
---

Rename the `@bazza-ui/react/adapters` subpath to `@bazza-ui/react/loaders`. The optional `QueryDependentLoaderConfig` type is now `QueryLoaderConfig`; update imports accordingly.
