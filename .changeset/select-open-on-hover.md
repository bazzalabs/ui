---
"@bazza-ui/react": minor
---

`Select.Trigger` now accepts `openOnHover`, `delay`, and `closeDelay` for parity with `DropdownMenu.Trigger`, forwarding them to the underlying Base UI `Popover.Trigger`.
