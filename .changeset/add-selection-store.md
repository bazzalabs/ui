---
"@bazza-ui/react": minor
---

Add internal `SelectionStore` — a pure selection engine (ordered-key range math, controlled/uncontrolled, prune invariant) exported at `@bazza-ui/react/internal/selection`.
