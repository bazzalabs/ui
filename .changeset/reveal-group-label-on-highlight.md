---
"@bazza-ui/react": patch
---

Keyboard navigation in `DropdownMenu`, `ContextMenu`, `Select`, and `Combobox` now reveals a group's label when the highlight lands on the group's first item, instead of aligning the scroll viewport to the item and leaving the label clipped above it.
