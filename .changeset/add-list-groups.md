---
"@bazza-ui/react": minor
---

Add `List.Group`, `List.GroupHeader`, and `List.GroupRows` with sticky collapsible headers; collapsed rows stay in the selection keyspace (ranges span them, selection is retained).
