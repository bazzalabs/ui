---
'@bazza-ui/react': minor
---

Add `rangeSelection`, `dragSelection`, and `getAriaSelectionText` props to the dropdown menu, context menu, and command menu roots, the `'range-selection'` and `'drag-selection'` change reasons, and a `data-pending` attribute on `CheckboxItem` for rows whose previewed checked state differs from the committed one. The gestures themselves land in follow-up changes.
