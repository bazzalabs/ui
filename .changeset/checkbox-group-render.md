---
'@bazza-ui/react': minor
---

Render data-first checkbox groups in lists, submenus, subpages, and deep-search results. Rows inside a `CheckboxGroupDef` read their checked state from the group automatically, so a custom `render` only needs to shape the container.
