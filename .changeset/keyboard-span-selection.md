---
'@bazza-ui/react': minor
---

Hold Shift while moving the highlight with the arrow keys, Home, or End to preview a span of checkbox items; releasing Shift commits it as one change. Escape cancels. This is the keyboard form of drag selection and follows the root's `rangeSelection` prop.
