---
"@bazza-ui/react": minor
---

Add the `List` layout primitive core (`@bazza-ui/react/layout/list`): composed collection + selection stores, `Root`/`Row`/`Cell` with named-column subgrid layout defaults, pointer selection, and `layout={false}` escape hatch.
