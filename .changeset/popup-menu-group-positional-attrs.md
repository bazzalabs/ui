---
'@bazza-ui/react': minor
---

Add `data-first-group` and `data-last-group` attributes to menu `Group`, `RadioGroup`, and `GroupLabel` parts. They mark the first/last visible group in a list and update with filtering.
