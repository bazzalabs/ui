---
'@bazza-ui/react': minor
---

Display group, radio-group, and separator nodes now carry their resolved menu node as `node`, with authored facts available at `node.def` and canonical identity at `node.id`. The old `group`, `radioGroup`, `separator`, and `resolvedNode` fields have been removed.
