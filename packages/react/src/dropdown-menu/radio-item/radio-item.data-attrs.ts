export enum DropdownMenuRadioItemDataAttributes {
  /**
   * Present when the radio item is checked/selected.
   */
  checked = 'data-checked',
  /**
   * Present when the radio item is not checked/selected.
   */
  unchecked = 'data-unchecked',
  /**
   * Present when the radio item is highlighted (via keyboard or pointer).
   */
  highlighted = 'data-highlighted',
  /**
   * Present when the radio item is disabled.
   */
  disabled = 'data-disabled',
}
