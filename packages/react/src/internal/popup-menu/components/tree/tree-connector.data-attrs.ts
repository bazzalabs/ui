export enum PopupMenuTreeConnectorDataAttributes {
  /**
   * Identifies the component part.
   * @type {'bazzaui-dropdown-menu-tree-connector' | 'bazzaui-context-menu-tree-connector'}
   */
  slot = 'bazzaui-[component]-tree-connector',
  /** The depth of the connector. */
  depth = 'data-depth',
}
