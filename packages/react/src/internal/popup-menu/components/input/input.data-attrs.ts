export enum PopupMenuInputDataAttributes {
  /**
   * Always present on the popup menu search input element.
   * Used by Surface auto-focus to target the library's own input
   * (never consumer-provided inputs inside the popup).
   */
  input = 'data-popup-menu-input',
}
