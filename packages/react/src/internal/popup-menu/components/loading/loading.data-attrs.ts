export enum PopupMenuLoadingDataAttributes {
  /**
   * Identifies the component part.
   * @type {'bazzaui-dropdown-menu-loading' | 'bazzaui-context-menu-loading' | 'bazzaui-select-loading' | 'bazzaui-combobox-loading'}
   */
  slot = 'bazzaui-[component]-loading',
  /**
   * Present when this row is the first visible list-level row.
   */
  first = 'data-first',
  /**
   * Present when this row is the last visible list-level row.
   */
  last = 'data-last',
}
