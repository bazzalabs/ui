export enum MenuRootDataAttributes {
  /**
   * Present when the menu is open.
   */
  open = 'data-open',
  /**
   * Present when the menu is closed.
   */
  closed = 'data-closed',
  /**
   * Present when the menu is disabled.
   */
  disabled = 'data-disabled',
}
